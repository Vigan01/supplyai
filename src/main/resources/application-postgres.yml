spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/supplyai}
    username: ${DB_USERNAME:supplyai}
    password: ${DB_PASSWORD:supplyai}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
